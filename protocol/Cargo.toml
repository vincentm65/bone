[package]
name = "bone-protocol"
version.workspace = true
edition.workspace = true

[lib]
name = "bone_protocol"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
num-format = "0.4.4"
