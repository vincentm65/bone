title: General
fields:
  - key: approval_mode
    label: approval_mode
    type: enum
    options: [safe, danger]
    default: safe
  - key: show_thinking
    label: "Show reasoning in a live pane"
    type: bool
    default: false
  - key: input_preset
    label: "Input style"
    type: enum
    options: [custom, lines, box, filled]
    default: custom

